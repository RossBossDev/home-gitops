apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: sabnzbd-config-claim
spec:
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 1Gi
    storageClassName: longhorn

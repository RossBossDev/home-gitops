apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: tautulli-config-claim
spec:
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 1Gi
    storageClassName: longhorn

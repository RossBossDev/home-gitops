apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: overseerr-config-claim
spec:
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 1Gi
    storageClassName: longhorn
